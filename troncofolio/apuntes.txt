Que raioz me falta:

-Agregar galeria y merch en el carrusel.
!Hacer una galeria de merch (excusa algo para no usar carro). (mandame correo o algo asi)
!(rapido)Agregar un popup de SignUp (funcion de Firebase) // Listo!!
-validar form de:
    -Login // del firebase
    -Register // del firebase
    -CommView
-Fabricar un AboutMe, una version extendida de la presentacion del carrusel.

-embellecer pagina(ultima prioridad)
